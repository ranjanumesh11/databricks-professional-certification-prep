site_name: Databricks Professional Certification Prep
site_description: Short docs for the Databricks training repo (Section 2 highlighted)

nav:
  - Home: index.md
  - "Section 2 — Data Modeling": 2-data-modeling.md

theme:
  name: material
  palette:
    - scheme: default
      primary: indigo
      accent: indigo
  font:
    text: Roboto
    code: Roboto Mono

# Dev-only custom CSS is kept in `docs/css/extra-dev.css` and should be
# enabled only on the `dev` branch. Do NOT reference it here to avoid
# applying dev-only styles to the production site.


markdown_extensions:
  - admonition
  - toc:
      permalink: true
  - pymdownx.superfences
  - pymdownx.tabbed
  - pymdownx.details
